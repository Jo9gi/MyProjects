{
  "name": "diabetic-retinopathy-system",
  "version": "1.0.0",
  "description": "Diabetic Retinopathy Prediction System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-all": "npm install && cd frontend-new && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
